# DataPrime Assistant - VM Deployment Environment Configuration
# Copy this file to .env.vm and fill in your actual values
# WARNING: Never commit the actual .env.vm file with secrets!

# ==============================================================================
# Coralogix Configuration (Company ID: 4015437)
# ==============================================================================
CX_TOKEN=your_coralogix_send_data_api_key_here
CX_DOMAIN=coralogix.com
CX_APPLICATION_NAME=dataprime-demo
CX_SUBSYSTEM_NAME=vm-deployment

# ==============================================================================
# OpenAI Configuration
# ==============================================================================
OPENAI_API_KEY=your_openai_api_key_here

# ==============================================================================
# Database Configuration (PostgreSQL)
# ==============================================================================
POSTGRES_USER=dataprime
POSTGRES_PASSWORD=generate_strong_password_here
POSTGRES_DB=dataprime_demo

# Full database URL (used by application services)
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://dataprime:generate_strong_password_here@postgres:5432/dataprime_demo

# ==============================================================================
# Redis Configuration
# ==============================================================================
REDIS_URL=redis://redis:6379/0

# ==============================================================================
# OpenTelemetry Configuration
# ==============================================================================
OTEL_EXPORTER_OTLP_ENDPOINT=http://otel-collector:4317
OTEL_EXPORTER_OTLP_PROTOCOL=grpc

# ==============================================================================
# Deployment Configuration
# ==============================================================================
ENVIRONMENT=production

# ==============================================================================
# Security Notes
# ==============================================================================
# 1. Generate a strong PostgreSQL password:
#    openssl rand -base64 32
#
# 2. Ensure proper file permissions:
#    chmod 600 .env.vm
#
# 3. Add .env.vm to .gitignore if not already present
#
# 4. For production, consider using:
#    - AWS Secrets Manager
#    - HashiCorp Vault
#    - Docker Secrets
#
# ==============================================================================
# Quick Start
# ==============================================================================
# 1. Copy this file: cp env.vm.example .env.vm
# 2. Edit .env.vm with your actual credentials
# 3. Generate PostgreSQL password: openssl rand -base64 32
# 4. Update DATABASE_URL with the same password
# 5. Start services: docker compose -f docker-compose.vm.yml up -d
#
# ==============================================================================

